{
  "name": "crea-ddf-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "MCP server for CREA/REALTOR.ca DDF (RESO Web API / OData) with compliance-safe tools.",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "node --enable-source-maps --loader tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --loader tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "lru-cache": "^11.0.0",
    "undici": "^6.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
